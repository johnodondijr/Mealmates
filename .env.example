# MealMates — Supabase (optional)
#
# Leave these unset to run fully on local device storage (with cross-tab
# realtime). Fill them in to switch to Supabase with live sync across devices.
#
# Find these in your Supabase project: Settings → API.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
